const string = "JavaScript is the best programming language";

const replacedString = string.replaceAll("JavaScript", "Python");

console.log(replacedString);

//Remplaza el contenido de la cadena de texto por el nuevo valor. El anterior por el nuevo
const users = {
    gndx: {
        name: 'Gndx',
    },
    ana: {
        country: 'Mexico',
    } 
}

console.log(users.gndx.name);
console.log(users?.bebeloper?.gndx?.name); //permite la verificación de elementos
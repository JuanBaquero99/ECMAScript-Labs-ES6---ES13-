console.log(window); //Es un objeto global que contiene todas las variables globales
console.log(globalThis); // globalThis es una variable global que apunta al objeto global
console.log(self); // self es una variable global que apunta al objeto global
